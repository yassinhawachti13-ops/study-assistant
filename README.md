# study_assistant

A simple Flask web app for solving equations, browsing PDF exercises, and
keeping a daily study log.

## Run locally

```bash
python main.py
```

Then open `http://localhost:5000` in a browser.

Add PDF exercises to `static/exercises/`. Daily study entries are stored in
`daily_log.json`.